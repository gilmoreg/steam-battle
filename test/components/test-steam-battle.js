/* import React from 'react';
import TestUtils from 'react-addons-test-utils';
import chai from 'chai';
const should = chai.should();
import SteamBattle from '../js/components/steam-battle';

describe('SteamBattle component', function() {
    it('should render the component properly', function() {
        const renderer = TestUtils.createRenderer();
        renderer.render(<SteamBattle />);
        const result = renderer.getRenderOutput();
        result.props.className.should.equal('steam-battle');
    })
}); */

